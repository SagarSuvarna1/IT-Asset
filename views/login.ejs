<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>IT Asset Management | Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
    }
    body {
      min-height: 100vh;
      width: 100vw;
      font-family: 'Montserrat', sans-serif;
      background:
        linear-gradient(180deg, rgba(14,35,67,0.88) 0%, rgba(28,56,99,0.78) 90%),
        url('https://images.unsplash.com/photo-1519389950473-47ba0277781c?auto=format&fit=crop&w=1350&q=80') center/cover no-repeat;
      display: flex;
      overflow: hidden;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .corner-links {
      position: absolute;
      top: 32px;
      right: 44px;
      display: flex;
      gap: 34px;
      font-size: 1.01rem;
    }
    .corner-links a {
      color: #fff;
      opacity: 0.83;
      text-decoration: none;
      font-weight: 500;
      letter-spacing: 0.4px;
      transition: color 0.18s;
    }
    .corner-links a:hover { color: #42d6ff; opacity: 1; }

    .login-center-box {
      background: rgba(28,42,68, 0.68);
      backdrop-filter: blur(12px) brightness(1.08);
      border-radius: 19px;
      box-shadow: 0 18px 80px -18px #131C38cc;
      padding: 56px 42px 40px;
      width: 370px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      z-index: 12;
    }
    .login-logo {
      margin-bottom: 32px;
      font-size: 2.8rem;
      color: #42d6ff;
      font-weight: 700;
      letter-spacing: 6px;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .login-logo .fa-network-wired {
      font-size: 2.6rem;
      color: #53defc;
      margin-bottom: 8px;
    }
    form {
      width: 100%;
      display: flex;
      flex-direction: column;
    }
    .input-field {
      display: flex;
      align-items: center;
      background: rgba(255,255,255,0.14);
      border-radius: 25px;
      padding: 0 18px;
      margin-bottom: 22px;
      border: 1.7px solid transparent;
      transition: border-color 0.2s;
    }
    .input-field:focus-within {
      border-color: #4ad2fc;
      background: rgba(255,255,255,0.20);
    }
    .input-field i {
      color: #46c1ffcc;
      font-size: 1.2rem;
      margin-right: 13px;
    }
    .input-field input, .input-field select {
      flex: 1;
      background: transparent;
      border: none;
      outline: none;
      color: #eaf6ff;
      font-family: inherit;
      font-size: 1.07rem;
      padding: 16px 0;
      letter-spacing: 0.25px;
    }
    .input-field select option { color: #122c4f; background: #eef9ff; }
    .login-btn {
      display: block;
      width: 100%;
      background-image: linear-gradient(90deg, #12caf8 0%, #228bdf 100%);
      border: none;
      border-radius: 30px;
      color: #fff;
      font-weight: 700;
      padding: 15px 0;
      font-size: 1.18rem;
      margin-top: 12px;
      margin-bottom: 8px;
      box-shadow: 0 6px 30px 0 #12caf888;
      cursor: pointer;
      letter-spacing: 1.1px;
      transition: background 0.16s, box-shadow 0.16s;
    }
    .login-btn:hover,
    .login-btn:focus {
      background-image: linear-gradient(90deg, #12caf8 70%, #1790ff 100%);
      box-shadow: 0 12px 32px 0 #25b6e9cc;
    }
    .bottom-links {
      width: 100%;
      display: flex;
      justify-content: space-between;
      font-size: 0.97rem;
      margin-top: 16px;
    }
    .bottom-links a {
      color: #aaf2ff;
      text-decoration: none;
      opacity: 0.86;
      transition: color 0.16s;
    }
    .bottom-links a:hover { color: #12caf8; opacity: 1; }
    @media (max-width: 600px) {
      .login-center-box { width: 97vw; padding: 32px 8vw 32px;}
      .corner-links { right: 16px; top: 17px; gap: 16px;}
    }
  </style>
</head>
<body>
  <div class="corner-links">
  
  </div>
  <div class="login-center-box">
    <div class="login-logo">
      <i class="fas fa-network-wired"></i>
      <span>IT AMS</span>
    </div>
    <form action="/login" method="POST" autocomplete="off">
      <div class="input-field">
        <i class="fas fa-building"></i>
        <select name="institution_id" required>
          <option value="" disabled selected>Institution</option>
          <% institutions.forEach(inst => { %>
            <option value="<%= inst.id %>"><%= inst.name %></option>
          <% }); %>
        </select>
      </div>
      <div class="input-field">
        <i class="fas fa-user"></i>
        <input type="text" name="username" placeholder="Username" required />
      </div>
      <div class="input-field">
        <i class="fas fa-lock"></i>
        <input type="password" name="password" placeholder="Password" required />
      </div>
      <button class="login-btn" type="submit">Sign In</button>
      <div class="bottom-links">
        <a href="#">             </a>
		 <a href="/settings">Forgot or Change password?</a>
      </div>
    </form>
  </div>
</body>
</html>

